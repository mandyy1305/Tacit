*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Vendored prebuilt native lib — large binary, re-download from the pinned release (see README).
app/libs/*.aar

# Firebase config (contains API key) — user drops this in locally
app/src/main/assets/google-services.json
