<?xml version="1.0" encoding="utf-8"?>
<!--
  Accessibility service config. Scoped to com.whatsapp only. We need window content to read
  the voice-note bubble's nodes (duration/timestamp text, the play control), and view-ids so
  the dump can show viewIdResourceName. flagIncludeNotImportantViews surfaces decorative nodes
  WhatsApp may mark unimportant; flagRetrieveInteractiveWindows helps reach the active window.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_desc"
    android:packageNames="com.whatsapp"
    android:accessibilityEventTypes="typeViewClicked|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true"
    android:accessibilityFlags="flagReportViewIds|flagIncludeNotImportantViews|flagRetrieveInteractiveWindows" />
